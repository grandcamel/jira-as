# api

- `api call` — Call OPERATION with its spec-derived flags; --help after OPERATION lists them.

- `api describe` — Show parameters, body outline, scope, notes and deprecation.

- `api search` — Find primary operations by ID, summary, tag, path or note.

- `api topics` — List topics supplied by enrichment.
